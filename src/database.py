from sqlalchemy import Column, Integer, String, ForeignKey, DateTime\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\n\nBase = declarative_base()\n\nclass Bet(Base):\n    __tablename__ = 'bets'\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'))\n    amount = Column(Integer)\n    created_at = Column(DateTime)\n    user = relationship('User', back_populates='bets')\n\nclass Trade(Base):\n    __tablename__ = 'trades'\n    id = Column(Integer, primary_key=True)\n    bet_id = Column(Integer, ForeignKey('bets.id'))\n    price = Column(Integer)\n    created_at = Column(DateTime)\n    bet = relationship('Bet', back_populates='trades')\n\nclass AlertHistory(Base):\n    __tablename__ = 'alert_history'\n    id = Column(Integer, primary_key=True)\n    alert_message = Column(String)\n    created_at = Column(DateTime)\n\n# Define relationships\nBet.trades = relationship('Trade', order_by=Trade.id, back_populates='bet')\nUser.bets = relationship('Bet', order_by=Bet.id, back_populates='user')\n